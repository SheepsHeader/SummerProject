spring:
  application:
    name: youngS
  #mysql连接配置
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/fdata?serverTimezone=UTC&useUnicode=true&characterEncoding=utf-8
    username: root
    password: root
mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.youngheart.domain
  configuration:
    #开启驼峰命名
    map-underscore-to-camel-case: true

